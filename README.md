# CineMatch

A lightweight movie recommendation web app using the OMDb API.

## Features
- Filter by genre, IMDb rating, age rating, and runtime
- Add personal favorites to improve recommendations
- Local caching for faster repeat searches
- "My Added Movies" carousel

## Run
Open `index.html` in a browser and paste your OMDb API key when prompted.
